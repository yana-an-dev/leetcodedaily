/**
 * @param {string} address
 * @return {string}
 */

const address = "255.100.50.0";

console.log(address.replaceAll(".", "[.]").toString());

// let defangIPaddr(address){() =>
//     console.log(address.replace('.', '[.]'))};
